The admin login credentials are currently being stored in Replit's local database instead of MongoDB. This means when the app is deployed, login fails because the deployed server uses MongoDB but credentials are not there.

## Fix Required:

### Step 1: Find where admin credentials are currently stored
- Check the authentication/login logic in the codebase
- Find if it is using Replit DB, local JSON file, or in-memory storage for user authentication
- Find the file that handles: POST /admin/login or similar auth route

### Step 2: Move credentials storage to MongoDB
- Admin user authentication must use the MongoDB database (same one used for feedback data)
- MongoDB URL: already configured in environment variables
- When admin logs in, it should check the 'users' collection in MongoDB for matching username and password

### Step 3: Create admin user in MongoDB on first startup
- Add a startup script or initialization function that checks if admin user exists in MongoDB 'users' collection
- If no admin user exists → automatically create one with these credentials:
  - username: "admin"
  - password: "shreerath_admin_2026" (store as bcrypt hash)
  - role: "admin"
- If admin user already exists → skip creation
- This ensures admin user is always available in MongoDB after any deployment or restart

### Step 4: Update the login route
- Login route must query MongoDB 'users' collection
- Compare entered password with bcrypt hashed password stored in MongoDB
- Do NOT use Replit DB, local storage, or hardcoded credentials anywhere

### Step 5: Update ecosystem/pm2 config if needed
- If there is an ecosystem.config.js or pm2 config file, make sure MongoDB URL and SESSION_SECRET are included as environment variables
- SESSION_SECRET should be a fixed string (not random) so sessions persist across restarts

## Rules:
- Do NOT change any frontend UI
- Do NOT touch feedback or overview page logic
- Only fix the authentication storage — move it from local/Replit DB to MongoDB
- Make sure this works both on Replit (development) and on the deployed server (production)
- Show which files you will edit before making changes